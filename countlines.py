# countlines.py
# 
# Count the number of lines in the solution.
# Takes a parse_text generator as an input and yield an integer.



def count_lines(parsed_text):
	counts = []
	for function in parsed_text:
		function.seek(0)
		return len()
